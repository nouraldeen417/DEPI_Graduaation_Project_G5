# Deployment target
# Allowed values: kubernetes, docker, both
DEPLOY_TARGET=both

# Pipeline action
# Allowed values: build & deploy, build only, deploy only
PIPELINE_ACTION="build & deploy"

# Custom build number or image tag
# Default: ${BUILD_NUMBER} (Jenkins build number, e.g., 42)
# Enter a string (e.g., v1.2.3) to override
CUSTOM_BUILD_NUMBER=${BUILD_NUMBER}

# Skip tests
# Allowed values: true, false
SKIP_TESTS=true
